Дата: 2026-04-03
Статус: draft
Спецификация: docs/2. GRISH-specifications/S01_demo_case_status_bot.md

# T03_s01_alevtina_service — AlevtinaService

## Customer-facing инкремент

На любое сообщение авторизованного клиента бот делает один GPT-вызов и получает структурированный JSON `{intent, answer}`. Базовая AI-инфраструктура готова к подключению всех интентов.

## Цель

Создать `AlevtinaService` — единственный AI-сервис демо-бота. Реализовать системный промпт (части 1 и 2), один OpenAI-вызов с `json_object`, парсинг ответа и фоллбэк.

## Контекст

Каркас (T01) готов. Авторизация (T02) выполняется параллельно. `AlevtinaService` независим от T02 — он не работает с сессиями напрямую, принимает готовые данные на вход. После объединения T02 и T03 в `text.py` собирается полный диалоговый флоу.

## Scope

- `demo_bot/services/alevtina_service.py` — класс `AlevtinaService`:
  - Конструктор принимает `openai_client` (тот же `AsyncOpenAI`, что у основного бота) и `model: str`
  - Метод `handle(user_message: str, history: list, case_context: str) -> dict`:
    - Собирает системный промпт из трёх частей (см. Технические детали)
    - Формирует `messages`: `history[-6]` + `{"role": "user", "content": user_message}`
    - Вызывает `openai_client.chat.completions.create(...)` с `response_format={"type": "json_object"}`
    - Парсит JSON ответ → возвращает `{"intent": str, "answer": str}`
    - При любом исключении (OpenAI недоступен, невалидный JSON) → возвращает `{"intent": "fallback", "answer": FALLBACK_ALEVTINA}`

**Системный промпт — Часть 1 (статичная, личность и правила):**
```
Ты — Алевтина, помощник клиента компании «Экспресс-Банкрот» по делу о банкротстве.

Ты отвечаешь СТРОГО на два типа вопросов:
1. Что происходит с делом клиента прямо сейчас
2. Что клиенту нужно сделать дальше

Всегда отвечай JSON: {"intent": "<тип>", "answer": "<текст>"}

Типы intent:
- case_status  — вопрос о статусе/состоянии дела
- next_steps   — вопрос о следующих шагах/действиях
- greeting     — приветствие
- gratitude    — благодарность
- off_topic    — всё остальное

Правила ответа:
— greeting: поздоровайся и предложи спросить о деле или шагах.
— gratitude: «Рада помочь! Если появятся вопросы — я здесь.»
— off_topic: «Я специализируюсь только на вашем деле и следующих шагах.»
— Никогда не выдумывай факты. Если данных нет — пиши «нет данных».
— Ответ лаконичный, структурированный, человечный. Без канцелярита.
— Ответ НЕ заканчивается вопросом к клиенту.
— ParseMode HTML: используй <b> для заголовков секций.
```

**Системный промпт — Часть 2 (статичная, знание БФЛ):**
```
ЭТАПЫ БАНКРОТСТВА ФИЗИЧЕСКОГО ЛИЦА И ДЕЙСТВИЯ КЛИЕНТА:

[C4:NEW] Сбор документов (до 14 дней)
  Клиент передаёт документы менеджеру. Без полного пакета дело не движется.
  Обязательные: паспорт, СНИЛС, ИНН.

[C4:UC_07IZ0U] Исковое заявление подготовлено
  Юрист готовит заявление. Клиент начинает копить:
  25 000 руб. — депозит арбитражного суда
  23 000 руб. — публикация финансового управляющего

[C4:PREPAYMENT_INVOICE] Заявление подано в суд
  Ожидание определения суда. Проверять на kad.arbitr.ru по ИНН.
  Суд может принять или оставить без движения.

[C4:11] Заявление оставлено без движения
  Суд запросил доп. документы — срочно передать менеджеру.
  Просрочка = риск отказа.

[C4:EXECUTING] Заявление принято судом, назначено заседание
  Оплатить депозит (25 000 руб.) и публикацию (23 000 руб.) по реквизитам от менеджера.
  Квитанции передать менеджеру.

[C4:12/13/17] Реструктуризация
  Долг не растёт. Коллекторы и приставы не беспокоят.
  НЕ пользоваться личными банковскими картами.
  Получать прожиточный минимум (ПМ) — инструкцию даёт менеджер.
  Финансовый управляющий (ФУ) контролирует счета.

[C4:14/15/18] Реализация имущества
  То же что реструктуризация. Плюс ФУ ведёт реализацию имущества при наличии.
  Клиент ничего не предпринимает самостоятельно.

[C4:WON] Долги списаны
  Суд вынес решение. Счета разблокируются в течение 10 дней.

[C4:2] Замороженная / [C4:UC_PQHMFA] Расторжение / [C4:LOSE] Закрыта
  Нестандартный статус. Рекомендуй связаться с менеджером напрямую.
```

**Системный промпт — Часть 3 (динамическая):**
```
ДАННЫЕ ДЕЛА КЛИЕНТА:
{case_context}
```

**Сборка системного промпта:**
```python
system_prompt = PART1_PERSONALITY + "\n\n" + PART2_BFL + "\n\nДАННЫЕ ДЕЛА КЛИЕНТА:\n" + case_context
```

**OpenAI-вызов:**
```python
response = await openai_client.chat.completions.create(
    model=model,
    messages=[{"role": "system", "content": system_prompt}] + history[-6:] + [{"role": "user", "content": user_message}],
    response_format={"type": "json_object"},
    temperature=0.3,
)
result = json.loads(response.choices[0].message.content)
return {"intent": result.get("intent", "off_topic"), "answer": result.get("answer", FALLBACK_ALEVTINA)}
```

## Out of scope

- Запись истории в БД — T06
- Кеширование `case_context` — T04
- Обработка конкретных интентов в `text.py` — T04–T07

## Технические детали

Модель: `OPENAI_MODEL` из env (gpt-4o-mini по умолчанию). Передаётся при инициализации `AlevtinaService` в `bot.py`.

`temperature=0.3` — балансирует между точностью и естественностью. Может быть скорректирован в T08.

Части промпта (PART1, PART2) — строковые константы в `alevtina_service.py`, не в `messages.py` (они специфичны для AI-логики, не для UI).

## Как протестировать

### 1. Базовый вызов

1. Создать экземпляр `AlevtinaService` с реальным OpenAI-клиентом
2. Вызвать `handle("Что с моим делом?", [], "Стадия: C4:EXECUTING. Суд: Арбитражный суд Москвы...")`
3. Проверить:
   - Результат — dict с ключами `intent` и `answer`
   - `intent == "case_status"`
   - `answer` содержит информацию из `case_context`, без выдуманных фактов

### 2. Фоллбэк при ошибке

1. Передать невалидный API ключ или заблокировать сеть
2. Вызвать `handle(...)`
3. Проверить: возвращается `{"intent": "fallback", "answer": FALLBACK_ALEVTINA}`, исключение не пробрасывается

### 3. Невалидный JSON от GPT

1. Временно подменить парсинг, чтобы симулировать `json.JSONDecodeError`
2. Проверить: фоллбэк возвращается корректно

## Критерии приёмки

1. `AlevtinaService.handle()` возвращает dict с `intent` и `answer` при успешном GPT-вызове
2. При любой ошибке (OpenAI, JSON) возвращается фоллбэк без краша
3. Системный промпт содержит все три части: личность + БФЛ + данные дела
4. `response_format={"type": "json_object"}` используется в запросе
5. В history передаётся не более 6 последних сообщений (history[-6:])

## Правила завершения задачи

**После акцепта задачи AI выполняет автоматически:**

1. **Обновить файл задачи:**
   - Статус → `✅ accepted`
   - Добавить дату завершения

2. **Переместить файл:**
   - Из: `docs/3. GRISH-tasks/S01_demo_case_status_bot/T03_s01_alevtina_service.md`
   - В: `docs/3. GRISH-tasks/Done/S01_demo_case_status_bot_done/T03_s01_alevtina_service_done.md`

3. **Обновить спецификацию:**
   - Статус T03 → `✅ done`

4. **Обновить GRISH-HANDOFF.md:**
   - Отметить T03 как выполненную
